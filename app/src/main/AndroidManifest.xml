<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.Lab2_20150632">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_calendario"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_calendario"
        android:supportsRtl="true"
        android:theme="@style/deufault">
        <activity android:name=".AgregarTareaActivity" android:parentActivityName=".TareasPendientes"/>
        <activity android:name=".TareasPendientes"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>